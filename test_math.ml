// Test math module imports
import math;

x = 16.0;
sq = math.sqrt(x);
print("Square root of 16: " + str(sq));

circle_area = math.pi * math.pow(5.0, 2.0);
print("Circle area (r=5): " + str(circle_area));

angle = math.sin(math.pi / 2.0);
print("sin(π/2): " + str(angle));
