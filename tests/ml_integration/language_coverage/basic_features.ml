// Variables and basic types test program (corrected ML syntax)
number = 42;
text = "Hello World";
boolean = true;
array = [1, 2, 3, 4, 5];

// Functions with correct ML syntax
function add(a, b) {
    return a + b;
}

function factorial(n) {
    if (n == 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}

// Test function calls
result1 = add(10, 20);
result2 = factorial(5);

// Basic operations
sum = 10 + 20;
difference = 50 - 30;
product = 6 * 7;
quotient = 84 / 2;