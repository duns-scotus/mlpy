// Simple slicing test
arr = [10, 20, 30, 40, 50];

// Basic slicing
slice1 = arr[1:3];
slice2 = arr[:2];
slice3 = arr[2:];

result = {
    arr: arr,
    slice1: slice1,
    slice2: slice2,
    slice3: slice3
};
